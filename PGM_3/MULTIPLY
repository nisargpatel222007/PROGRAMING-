#include <stdio.h>
 int main()
 {
 int a, b, c_multiply;
 printf( " NISARG PATEL \n" );
 printf( " Enter two numbers: " );
 scanf("%d %d", &a, &b);
 c_multiply = a * b;
 printf("product = %d\n", c_multiply);
 }
