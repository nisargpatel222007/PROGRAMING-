#include <stdio.h>
 int main()
 {
 int hour,minutes;
 printf( " NISARG PATEL \n" );
 printf( " Enter time inn hour : " );
 scanf("%d", &hour);
 minutes = hour * 60;
 printf ( "minutes = %d\n ",minutes );
 }
